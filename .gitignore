# OS / editor junk
.DS_Store

# Python runtime / build
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.db
uploads/

# Harness scratch area: throwaway review copies live here
projects/.bench/

# Bulky raw per-run dumps (auditing only); committed reports + results stay tracked
harness/reports/raw/
